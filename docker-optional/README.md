# Docker (Optionnel)

Les fichiers Docker sont disponibles ici pour ceux qui souhaitent utiliser Docker.

Pour utiliser le projet sans Docker, voir le README principal et utiliser `make` ou les scripts shell.

## Utilisation Docker (optionnel)

```bash
# Depuis la racine du projet
docker-compose up --build
```
